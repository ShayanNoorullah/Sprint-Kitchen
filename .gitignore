# EmailJS credentials — never commit the real config file
Web Application/emailjs-config.js

# Deployment build output
deploy/

# OS / editor artifacts
.DS_Store
Thumbs.db
desktop.ini
*.suo
.vscode/settings.json
